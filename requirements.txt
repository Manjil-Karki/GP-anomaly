# ── PyTorch CUDA wheels (pulled from PyTorch's own index) ──────────────────
# Driver reports CUDA 13.0; cu124 wheels are forward-compatible with it.
--extra-index-url https://download.pytorch.org/whl/cu124

torch>=2.3.0
torchvision>=0.18.0

# ── Gaussian Process ────────────────────────────────────────────────────────
gpytorch>=1.11

# ── Feature extraction backbone (DINOv2 ViT-B/14) ──────────────────────────
timm>=0.9

# ── Core data & numerics ────────────────────────────────────────────────────
numpy==2.2.6
pandas==2.3.3
scipy==1.15.3
Pillow==12.3.0

# ── Visualisation ───────────────────────────────────────────────────────────
matplotlib==3.10.9
seaborn==0.13.2

# ── Machine learning utilities ──────────────────────────────────────────────
scikit-learn>=1.3
tqdm>=4.65

# ── Jupyter ─────────────────────────────────────────────────────────────────
jupyter==1.1.1
jupyterlab==4.6.3
ipykernel==7.3.0
notebook==7.6.2
nbformat>=5.9
nbconvert>=7.0

# ── Anomaly detection baselines (heavy; comment out if GPU memory < 8 GB) ──
anomalib>=1.0
